export interface Product {
  id: string,
  category: string,
  brand: string,
  releaseDate: string,
  color: string,
  model: string,
  price: number,
  capacity: string,
  size?: string,
  img: string
  inStockCount: number,
}
